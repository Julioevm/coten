[project]
name = "coten"
version = "0.1.0"
description = "Castle of the Eternal Night — A classic roguelike"
readme = "readme.md"
license = { file = "LICENSE.md" }
requires-python = ">=3.11,<3.13"
dependencies = [
    "tcod>=16.2.1,<17",
    "numpy>=1.26.2,<2",
]

[dependency-groups]
dev = [
    "nuitka>=2.0.2",
]

[tool.uv]
package = false
